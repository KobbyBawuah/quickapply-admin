You are working on the existing QuickApply Pro Admin Console.

Do not rebuild from scratch. Keep the current app, current UI structure, current sidebar, current MongoDB connection, current Gmail/Nodemailer email sending, existing users/campaign/logs/settings pages, and existing backend APIs.

Now upgrade the system with AI-generated email and newsletter workflows using Claude AI API.

CLIENT NEW REQUIREMENTS

The client wants:
1. AI-generated marketing emails.
2. AI-generated newsletters with professional graphics/layout.
3. Approval workflow before anything is sent.
4. Approved content library.
5. Auto-generate new email drafts every 2 days.
6. Auto-generate new newsletter drafts every 2 days.
7. Manual “Generate Now with AI” button.
8. Approved inactive-user email templates should be used for inactive user campaigns.
9. Approved newsletter templates should be used for newsletter campaigns every 14 days.
10. Discount code and discount link should be configurable in Settings.
11. Newsletter content must dynamically use discount code and CTA/discount link from Settings.
12. The generated copy must match QuickApply Pro’s website/brand positioning.
13. Newsletters must not be plain text only. They must look like professional visual email newsletters with sections, colors, cards, CTA buttons, feature blocks, discount block, and optional image/graphic placeholders.
14. Emails and newsletters should be generated by Claude using the maximum/highest available Claude model configured by API key.

BRAND CONTEXT FROM QUICKAPPLY PRO WEBSITE

QuickApply Pro is an AI-powered job application platform.

Core product positioning:
- Job application co-pilot.
- Helps users land more interviews faster.
- AutoApply to many jobs with AI-tailored resumes and cover letters.
- Chrome extension helps autofill job application forms.
- Web app provides the full toolkit.
- Resume generation and tailoring in seconds.
- ATS-friendly resumes.
- Cover letter generation.
- Resume grading against job descriptions.
- Interview preparation.
- Job fraud/scam detection.
- LinkedIn profile optimization.
- Job recommendations.
- Supports international job seekers.
- Affordable compared to expensive job-search tools.
- Helps users save time without lowering application quality.
- Focus on working professionals, active job seekers, career switchers, recent grads, and international applicants.
- Brand voice: sharp, confident, clean, direct, practical, not desperate, not spammy, not overhyped.

Avoid these words:
- game-changer
- dream job
- supercharge
- cutting-edge
- guaranteed
- effortless
- buy now
- limited offer
- free money
- miracle
- secret trick

Do not overpromise job outcomes.
Do not claim guaranteed interviews or guaranteed hiring.
Do not imply QuickApply Pro fabricates credentials.
QuickApply Pro strengthens what is real.

CURRENT APP CONTEXT

Existing app already has:
- Dashboard
- Users page
- Campaigns
- Inactive User Campaign
- Newsletter Campaign
- Run History
- Templates
- Email Logs
- Settings
- MongoDB connection
- Gmail/Nodemailer test email
- Campaign sending
- Email logs
- Campaign runs
- User filtering
- America/New_York timezone

Keep all of that functional.

NEW FEATURE: CLAUDE AI SETTINGS

Add a new section in Settings called “AI Generation Settings”.

Fields:
- Claude API Key
- Claude Model
- Enable AI Auto Generation toggle
- Auto-generate email drafts every X days
- Auto-generate newsletter drafts every X days
- Number of email drafts to generate per run
- Number of newsletter drafts to generate per run
- Default campaign audience
- Discount Code
- Discount Percentage/Text
- Discount URL
- Discount Expiry Date
- Brand Notes / Extra Instructions
- Newsletter Primary Color
- Newsletter Accent Color
- Newsletter Header Image URL optional
- Newsletter Footer Text
- Company Website URL

Default values:
CLAUDE_MODEL=claude-3-5-sonnet-latest or highest available model supported by the SDK/API
AI_AUTO_GENERATION_ENABLED=true
AI_GENERATE_INTERVAL_DAYS=2
AI_EMAIL_DRAFTS_PER_RUN=2
AI_NEWSLETTER_DRAFTS_PER_RUN=1
DISCOUNT_CODE=QAP20
DISCOUNT_TEXT=20% off your next upgrade
DISCOUNT_URL=https://quickapplypro.com/pricing
COMPANY_WEBSITE_URL=https://quickapplypro.com
NEWSLETTER_PRIMARY_COLOR=#2563eb
NEWSLETTER_ACCENT_COLOR=#f97316
CRON_TIMEZONE=America/New_York

Important:
- Claude API key must never be exposed to frontend.
- Store Claude API key securely in backend env or encrypted settings.
- Frontend should only show masked API key state.
- If user leaves Claude API key blank when saving settings, keep the previous key.
- Never log Claude API key in backend logs.
- Never send Claude API key to frontend API responses.

ENV VARIABLES TO SUPPORT

Add support for:

CLAUDE_API_KEY=
CLAUDE_MODEL=claude-3-5-sonnet-latest
AI_AUTO_GENERATION_ENABLED=true
AI_GENERATE_INTERVAL_DAYS=2
AI_EMAIL_DRAFTS_PER_RUN=2
AI_NEWSLETTER_DRAFTS_PER_RUN=1

DISCOUNT_CODE=QAP20
DISCOUNT_TEXT=20% off your next upgrade
DISCOUNT_URL=https://quickapplypro.com/pricing
DISCOUNT_EXPIRY_DATE=
COMPANY_WEBSITE_URL=https://quickapplypro.com
NEWSLETTER_PRIMARY_COLOR=#2563eb
NEWSLETTER_ACCENT_COLOR=#f97316
NEWSLETTER_HEADER_IMAGE_URL=
NEWSLETTER_FOOTER_TEXT=You are receiving this because you signed up for QuickApply Pro.

DATA MODELS / COLLECTIONS

Create or update these collections:

1. ai_content_drafts

Fields:
{
  _id,
  contentType: "inactive_email" | "newsletter",
  title,
  subject,
  preheader,
  htmlBody,
  textBody,
  status: "draft" | "pending_approval" | "approved" | "rejected" | "archived",
  generationSource: "manual" | "scheduled",
  aiProvider: "claude",
  aiModel,
  promptUsed,
  targetAudience,
  angle,
  discountCode,
  discountText,
  discountUrl,
  ctaUrl,
  createdAt,
  updatedAt,
  approvedAt,
  approvedBy,
  rejectedAt,
  rejectionReason,
  usageCount,
  lastUsedAt,
  metadata
}

2. approved_content_templates

Fields:
{
  _id,
  contentType: "inactive_email" | "newsletter",
  draftId,
  title,
  subject,
  preheader,
  htmlBody,
  textBody,
  status: "active" | "inactive" | "archived",
  source: "ai_approved",
  angle,
  discountCode,
  discountText,
  discountUrl,
  ctaUrl,
  createdAt,
  updatedAt,
  approvedAt,
  usageCount,
  lastUsedAt
}

3. ai_generation_runs

Fields:
{
  _id,
  runType: "manual" | "scheduled",
  contentType: "inactive_email" | "newsletter" | "both",
  startedAt,
  finishedAt,
  status: "running" | "completed" | "failed",
  generatedEmailDrafts,
  generatedNewsletterDrafts,
  failedCount,
  errorMessage,
  aiModel,
  notes
}

4. app_settings update with:
{
  claudeApiKeyEncryptedOrMasked,
  claudeModel,
  aiAutoGenerationEnabled,
  aiGenerateIntervalDays,
  aiEmailDraftsPerRun,
  aiNewsletterDraftsPerRun,
  discountCode,
  discountText,
  discountUrl,
  discountExpiryDate,
  companyWebsiteUrl,
  newsletterPrimaryColor,
  newsletterAccentColor,
  newsletterHeaderImageUrl,
  newsletterFooterText,
  brandNotes
}

NEW DASHBOARD PAGES

Add these new pages to sidebar:

1. AI Generator
2. Approval Queue
3. Approved Templates

Sidebar structure should become:

Dashboard
Users
Campaigns
  - Inactive Users
  - Newsletter
  - Run History
AI Content
  - AI Generator
  - Approval Queue
  - Approved Templates
Templates
Email Logs
Settings

PAGE 1: AI GENERATOR

Purpose:
Manual generation area where client can generate emails/newsletters using Claude.

Sections:
- Generate Inactive Email Drafts
- Generate Newsletter Drafts
- Generate Both

Controls:
- Content type dropdown:
  - Inactive-user promotional email
  - Newsletter
  - Both
- Number of drafts
- Audience dropdown:
  - inactive users
  - free users
  - trial users
  - expired users
  - all users
  - international job seekers
  - working professionals
  - recent graduates
- Angle dropdown:
  - Sunday night application anxiety
  - Working professional bored in current role
  - Applying to many jobs and hearing nothing
  - Resume tailoring in under 60 seconds
  - Chrome extension autofill
  - Interview prep
  - Job fraud/scam detection
  - Affordable vs competitors
  - International job seekers
  - Job market is moving fast
  - Resume quality vs mass applying
- Discount code toggle
- Custom instruction textarea
- Generate Now button

When Generate Now is clicked:
- Backend calls Claude API.
- Generated content saved in ai_content_drafts with status pending_approval.
- UI shows generated drafts in a preview panel.
- Nothing is sent automatically.
- User can click View, Edit, Approve, Reject.

PAGE 2: APPROVAL QUEUE

Purpose:
Client reviews AI-generated content before it becomes usable.

Show tabs:
- All Pending
- Email Drafts
- Newsletter Drafts
- Rejected
- Approved Recently

Table/Card fields:
- Content type
- Title
- Subject
- Angle
- Created date
- AI model
- Status
- Actions

Actions:
- Preview
- Edit
- Approve
- Reject
- Archive
- Regenerate Similar
- Send Test Email

Approval behavior:
- If approved inactive email:
  - Copy into approved_content_templates
  - Also optionally sync into email_templates if existing system requires it.
  - Mark draft status approved.
- If approved newsletter:
  - Copy into approved_content_templates.
  - Also optionally sync into email_templates if existing system requires it.
  - Mark draft status approved.
- Approved content becomes available in campaign template dropdowns.

Reject behavior:
- Ask for rejection reason.
- Set status rejected.
- Do not delete draft.

Edit behavior:
- User can edit subject, preheader, HTML body, text body, discount block, CTA text, CTA URL.
- Save edited draft.
- Then approve.

PAGE 3: APPROVED TEMPLATES

Purpose:
Approved AI-generated templates library.

Show:
- Approved inactive emails
- Approved newsletters
- Active/inactive status
- Last used
- Usage count
- Preview
- Send test
- Set as default
- Deactivate
- Archive

Campaign pages should pull from approved templates:
- Inactive Campaign template dropdown must show approved inactive_email templates.
- Newsletter Campaign template dropdown must show approved newsletter templates.
- Auto-select should choose latest active approved template.
- If no approved template exists, show warning and allow fallback to default template.

AI EMAIL GENERATION REQUIREMENTS

Inactive-user emails must:
- Be short and direct.
- Have strong subject line.
- Include preheader.
- Personalize using variables.
- Use QAP features naturally.
- Include one primary CTA.
- Include discount code if enabled.
- Include unsubscribe/do-not-contact footer.
- Be HTML + plain text.
- Avoid spam words.
- Avoid overpromising.
- Use variables:
  {{firstName}}
  {{name}}
  {{daysInactive}}
  {{lastLoginDate}}
  {{plan}}
  {{subscriptionStatus}}
  {{ctaUrl}}
  {{discountCode}}
  {{discountText}}
  {{discountUrl}}

Example email concept:
Subject: Your next application should not take all night
Preheader: Tailor the resume, prep the interview, and move faster.

Hi {{firstName}},

A role can look worth applying to until the application starts eating your evening.

QuickApply Pro helps you move faster without sending the same resume everywhere:
- Tailor your resume to the role
- Generate a focused cover letter
- Autofill forms with the Chrome extension
- Check job posts for scam signals
- Prep before the callback

Use code {{discountCode}} for {{discountText}}.

CTA: Continue with QuickApply Pro

AI NEWSLETTER GENERATION REQUIREMENTS

Newsletter must be professional, visual, and email-client friendly.

It should not look like only paragraphs.

Generate both:
1. HTML newsletter
2. Plain text fallback

Newsletter HTML must be table-based or email-safe HTML.
Use inline CSS.
Avoid external CSS files.
Avoid JavaScript.
Use safe layout for Gmail/Outlook.
Max width: 640px.
Responsive-friendly.
Use sections/cards.

Newsletter design should include:
- Branded header
- Hero section
- Main headline
- Short intro
- Visual feature cards
- “This week’s job-search insight” section
- QAP feature highlight section
- Discount/promo block
- CTA button
- Footer
- Unsubscribe/do-not-contact placeholder

Newsletter should use:
- Primary color from settings
- Accent color from settings
- Logo/name: QuickApply Pro
- Optional header image URL from settings
- Discount code from settings
- Discount URL from settings
- CTA URL from settings
- Footer text from settings

Newsletter style examples:
Professional ecommerce/newsletter look:
- colored header
- discount block
- feature cards
- section dividers
- CTA button
- small image/graphic placeholders
- clean typography

Newsletter content angles:
- Why job applications feel harder in 2026
- Why tailored resumes beat mass applying
- Why applying to 50 jobs with one resume does not work
- Why international job seekers need stronger application materials
- How fake job postings waste serious candidates’ time
- Why working professionals should keep an application kit ready
- How to turn Sunday night job-search anxiety into a focused plan
- What to do before a promising role closes
- Why speed matters only when quality stays high

Newsletter must include a discount block if discount settings exist:
Example:
“Use code {{discountCode}}”
“{{discountText}}”
Button: Claim Discount
URL: {{discountUrl}}

Do not say guaranteed interviews.
Do not say guaranteed job.
Do not use fake urgency unless discount expiry exists.

CLAUDE PROMPTING REQUIREMENTS

Create backend Claude service:
- services/claudeService.ts or services/aiGenerationService.ts
- Function generateInactiveEmailDrafts(options)
- Function generateNewsletterDrafts(options)
- Function generateBoth(options)
- Function buildBrandContext()
- Function buildEmailPrompt()
- Function buildNewsletterPrompt()
- Function parseClaudeResponse()

Claude must return strict JSON.

For inactive email generation, require Claude JSON:
{
  "drafts": [
    {
      "title": "...",
      "subject": "...",
      "preheader": "...",
      "angle": "...",
      "htmlBody": "...",
      "textBody": "..."
    }
  ]
}

For newsletter generation, require Claude JSON:
{
  "drafts": [
    {
      "title": "...",
      "subject": "...",
      "preheader": "...",
      "angle": "...",
      "htmlBody": "...",
      "textBody": "..."
    }
  ]
}

If Claude returns invalid JSON:
- Retry once with a JSON repair prompt.
- If still invalid, save generation run as failed.
- Show clear error to user.

AI GENERATION SCHEDULE

Add cron job:
- Every 2 days at 8:00 AM America/New_York
- If AI auto-generation enabled:
  - Generate configured number of inactive email drafts.
  - Generate configured number of newsletter drafts.
  - Save them as pending_approval.
  - Do not send anything.
  - Create ai_generation_runs entry.

Cron env:
AI_GENERATION_CRON=0 8 */2 * *

Manual generation:
POST /api/ai/generate

Scheduled generation:
runAiGenerationCron()

APPROVAL BEFORE SENDING

Critical rule:
AI-generated content must never be sent automatically before approval.

Campaign sending logic:
- Inactive campaign must use only approved active inactive_email templates.
- Newsletter campaign must use only approved active newsletter templates.
- If no approved active template exists:
  - Do not run campaign.
  - Show error: “No approved template available. Approve a template first.”
- If template override is selected:
  - It must be from approved active templates only.

API ROUTES TO ADD

AI:
GET /api/ai/drafts
POST /api/ai/generate
GET /api/ai/drafts/:id
PUT /api/ai/drafts/:id
POST /api/ai/drafts/:id/approve
POST /api/ai/drafts/:id/reject
POST /api/ai/drafts/:id/archive
POST /api/ai/drafts/:id/regenerate-similar
POST /api/ai/drafts/:id/send-test
GET /api/ai/generation-runs

Approved Templates:
GET /api/approved-templates
GET /api/approved-templates/:id
PUT /api/approved-templates/:id
POST /api/approved-templates/:id/set-default
POST /api/approved-templates/:id/deactivate
POST /api/approved-templates/:id/archive
POST /api/approved-templates/:id/send-test

Settings:
Update existing settings API to support:
- Claude API Key
- Claude Model
- AI auto generation
- Discount code
- Discount text
- Discount URL
- Discount expiry
- Newsletter colors
- Header image URL
- Brand notes

Campaigns:
Update existing campaign APIs:
- Inactive campaign template dropdown must load approved inactive_email templates.
- Newsletter template dropdown must load approved newsletter templates.
- Campaign runs must reject unapproved templates.

DEBUG ENDPOINTS UPDATE

Update /api/debug/status to include:
- Claude API configured: true/false
- Claude model
- AI auto generation enabled
- AI generation interval days
- pending AI drafts count
- approved inactive email templates count
- approved newsletter templates count
- discount code
- discount URL
- newsletter primary/accent colors

Update /api/debug/test-all to include:
- Claude config check
- AI drafts collection check
- approved templates collection check
- generation runs collection check
- approval workflow check
- campaign template approval enforcement check

FRONTEND UI DETAILS

AI Generator page:
- Modern cards
- Generate Now button
- Loading state while Claude generates
- Show generated drafts immediately
- Preview modal
- Edit modal
- Approve/Reject buttons

Approval Queue page:
- Card/table hybrid layout
- Status badges
- Preview modal
- Rich HTML preview
- Plain text tab
- Approve/Reject/Edit/Archive actions

Approved Templates page:
- Template cards
- Filter by content type
- Search by title/subject
- Preview
- Send test
- Set default
- Deactivate
- Archive

Newsletter Preview:
Render HTML in a safe preview iframe or sanitized container.
Do not execute scripts.

Settings:
Add “AI Generation Settings” section.
Add “Discount & Offer Settings” section.
Add “Newsletter Design Settings” section.

Campaign pages:
Update dropdowns to say:
- Auto-select latest approved template
- Show approved templates only

TESTING REQUIREMENTS

After implementing, test these flows:

1. Add Claude API key in Settings.
2. Save settings.
3. Generate inactive email draft manually.
4. Confirm draft appears in Approval Queue.
5. Preview draft.
6. Edit draft.
7. Approve draft.
8. Confirm it appears in Approved Templates.
9. Confirm inactive campaign dropdown shows it.
10. Run inactive campaign with MAX_EMAILS_PER_RUN=1.
11. Confirm campaign uses approved template only.
12. Generate newsletter draft manually.
13. Confirm newsletter has visual HTML design with header/cards/discount/CTA.
14. Approve newsletter.
15. Confirm newsletter campaign dropdown shows it.
16. Run newsletter with MAX_EMAILS_PER_RUN=1.
17. Confirm email_logs and campaign_runs are created.
18. Confirm unapproved drafts cannot be sent by campaigns.
19. Confirm discount code from Settings appears in generated newsletter.
20. Confirm changing discount code in Settings affects newly generated drafts.
21. Confirm AI auto-generation cron creates pending drafts but does not send.
22. Confirm /api/debug/status and /api/debug/test-all pass.

IMPORTANT SAFETY

- Do not bulk send during testing.
- Use MAX_EMAILS_PER_RUN=1 for test campaigns.
- Do not expose secrets in frontend.
- Do not send unapproved AI content.
- Do not auto-approve AI drafts.
- Do not auto-send newly generated drafts.
- Use approval workflow strictly.

FINAL ACCEPTANCE CRITERIA

The final app must support:

1. Claude AI content generation.
2. Manual Generate Now for emails.
3. Manual Generate Now for newsletters.
4. Scheduled AI draft generation every 2 days.
5. Pending approval queue.
6. Approve/reject/edit AI-generated content.
7. Approved templates library.
8. Inactive campaign uses only approved inactive email templates.
9. Newsletter campaign uses only approved newsletter templates.
10. Newsletter has professional visual HTML layout with colors, cards, discount block, CTA, footer.
11. Discount code and discount URL come dynamically from Settings.
12. Test email works for AI drafts and approved templates.
13. All logs and campaign runs continue working.
14. Existing dashboard/users/settings/email logs/run history must not break.
15. All UI buttons must be functional.